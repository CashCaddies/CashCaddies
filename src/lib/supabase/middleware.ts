/**
 * Supabase session refresh runs in the root `middleware.ts` (repo root)
 * via `createServerClient` + `getUser()`. This file is kept as a stub so
 * imports from earlier experiments fail loudly if reintroduced.
 */
export {};
